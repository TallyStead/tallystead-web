export type BillProfile = { bill_profile_id: string; name: string; payee: string | null; cadence: string; next_due_date: string | null; expected_amount_minor: number; minimum_amount_minor: number | null; maximum_amount_minor: number | null; currency_code: string; is_essential: boolean; priority: number; is_active: boolean };
export type Debt = { debt_id: string; name: string; lender: string | null; account_id: string | null; balance_minor: number; balance_anchor_minor: number; balance_as_of_date: string | null; apr_basis_points: number; minimum_payment_minor: number; due_day: number; next_due_date: string; currency_code: string; is_active: boolean };
export type PaymentLink = { payment_link_id: string; transaction_id: string; amount_minor: number };
export type BillInstance = { bill_instance_id: string; bill_profile_id: string | null; debt_id: string | null; name: string; due_date: string; expected_amount_minor: number; minimum_amount_minor: number | null; maximum_amount_minor: number | null; paid_amount_minor: number; currency_code: string; is_essential: boolean; priority: number; status: string; note: string | null; payment_links: PaymentLink[] };
export type IncomeSource = { income_source_id: string; name: string; payer: string | null; cadence: string; next_expected_date: string | null; expected_amount_minor: number; minimum_amount_minor: number | null; maximum_amount_minor: number | null; currency_code: string; confidence_percent: number; is_active: boolean };
export type IncomeEvent = { income_event_id: string; income_source_id: string | null; received_transaction_id: string | null; name: string; expected_date: string; expected_amount_minor: number; minimum_amount_minor: number | null; maximum_amount_minor: number | null; currency_code: string; confidence_percent: number; status: string; note: string | null };
export type CalendarItem = { item_type: "bill" | "debt" | "income"; item_id: string; name: string; event_date: string; amount_minor: number; currency_code: string; status: string; priority: number | null };
